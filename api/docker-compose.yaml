# Use root/example as user/password credentials

services:
  db:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: HOTpeper1!
      MYSQL_DATABASE: FeatureTrack
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
